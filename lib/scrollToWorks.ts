/** Session flag so home can scroll to #works after App Router client navigation (hash often not applied in time). */
export const SCROLL_TO_WORKS_STORAGE_KEY = "portfolio:scrollToWorks";
/** Session flag so home can scroll to #snippets after App Router client navigation. */
export const SCROLL_TO_SNIPPETS_STORAGE_KEY = "portfolio:scrollToSnippets";
